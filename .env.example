# Only the selected API provider needs its key.
BRAVE_SEARCH_API_KEY=
TAVILY_API_KEY=
GEMINI_API_KEY=

# Optional. Used only when SearXNG is selected and no URL is saved in settings.
SEARXNG_BASE_URL=http://127.0.0.1:8080
